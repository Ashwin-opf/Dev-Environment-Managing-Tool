{
  "name": "pc-doctor-frontend",
  "version": "0.1.0",
  "description": "Frontend for Intelligent PC Doctor",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node -e \"const fs = require('fs'); ['main.js', 'animations.js', 'gsap.min.js', 'ScrollTrigger.min.js'].forEach(f => fs.copyFileSync(f, 'dist/' + f))\""
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0",
    "vite": "^5.0.0"
  },
  "author": "",
  "license": "MIT"
}
